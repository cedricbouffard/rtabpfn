% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parsnip-timeseries.R
\name{tab_pfn_ts}
\alias{tab_pfn_ts}
\alias{get_encoding.tab_pfn_ts}
\alias{update.tab_pfn_ts}
\title{TabPFN Time Series Model for tidymodels}
\usage{
get_encoding.tab_pfn_ts()

tab_pfn_ts(
  mode = "regression",
  engine = "tabpfn_ts",
  prediction_length = 12,
  quantiles = c(0.1, 0.5, 0.9),
  tabpfn_output_selection = "median",
  date_col = NULL,
  value_col = NULL,
  item_id_col = NULL,
  ...
)

\method{update}{tab_pfn_ts}(
  object,
  parameters = NULL,
  prediction_length = NULL,
  quantiles = NULL,
  tabpfn_output_selection = NULL,
  date_col = NULL,
  value_col = NULL,
  item_id_col = NULL,
  fresh = FALSE,
  ...
)
}
\arguments{
\item{mode}{A single character string for the prediction mode. Only "regression" is supported for time series.}

\item{engine}{A single character string specifying the computational engine. Always "tabpfn_ts" for time series.}

\item{prediction_length}{Number of steps to forecast ahead (integer)}

\item{quantiles}{Numeric vector of quantiles for probabilistic forecasting}

\item{tabpfn_output_selection}{Output selection: "median" or "mean"}

\item{date_col}{Name of the date/datetime column (NULL for auto-detection)}

\item{value_col}{Name of the value column (NULL for auto-detection)}

\item{item_id_col}{Name of the item_id column for multiple time series (NULL for single series)}

\item{...}{Additional engine-specific arguments}
}
\value{
A tibble with encoding information

A model specification object
}
\description{
A TabPFN Time Series (TabPFN-TS) model for zero-shot forecasting
  with the tidymodels ecosystem (parsnip, workflows, tune, etc.)
Get encoding for tab_pfn_ts model
}
\examples{
\dontrun{
library(tidymodels)
library(rtabpfn)

# Time series forecasting model
ts_spec <- tab_pfn_ts(mode = "regression") \%>\%
  set_engine("tabpfn_ts") \%>\%
  set_args(prediction_length = 12, quantiles = c(0.1, 0.5, 0.9))
}
}
\keyword{internal}

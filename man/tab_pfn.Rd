% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parsnip-tabpfn.R
\name{tab_pfn}
\alias{tab_pfn}
\alias{get_encoding.tab_pfn}
\alias{update.tab_pfn}
\title{TabPFN Model for tidymodels}
\usage{
get_encoding.tab_pfn()

tab_pfn(
  mode = "unknown",
  engine = "tabpfn",
  N_ensemble_configurations = 8,
  max_len_feature_basis = 1024,
  device = "auto",
  ...
)

\method{update}{tab_pfn}(
  object,
  parameters = NULL,
  N_ensemble_configurations = NULL,
  max_len_feature_basis = NULL,
  device = NULL,
  fresh = FALSE,
  ...
)
}
\arguments{
\item{mode}{A single character string for the prediction mode: "classification" or "regression"}

\item{engine}{A single character string specifying the computational engine. For TabPFN, this is always "tabpfn"}

\item{N_ensemble_configurations}{Number of ensemble configurations (integer)}

\item{max_len_feature_basis}{Maximum length of feature basis (integer)}

\item{device}{Device to use: "auto", "cpu", or "cuda"}

\item{...}{Additional engine-specific arguments}
}
\value{
A tibble with encoding information

A model specification object
}
\description{
A TabPFN (Tabular Prior-Fitted Network) model that can be used
  with the tidymodels ecosystem (parsnip, workflows, tune, etc.)
Get encoding for tab_pfn model
}
\examples{
\dontrun{
library(tidymodels)

# Regression model
tab_pfn_reg_spec <- tab_pfn(mode = "regression") \%>\%
  set_engine("tabpfn") \%>\%
  fit(mpg ~ ., data = mtcars)

# Classification model
tab_pfn_cls_spec <- tab_pfn(mode = "classification") \%>\%
  set_engine("tabpfn") \%>\%
  fit(Species ~ ., data = iris)
}
}
\keyword{internal}

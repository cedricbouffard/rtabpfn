% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anomaly.R
\name{tab_pfn_unsupervised}
\alias{tab_pfn_unsupervised}
\title{Train a TabPFN Unsupervised Anomaly Detection Model}
\usage{
tab_pfn_unsupervised(
  X,
  n_estimators = 4,
  device = "auto",
  categorical_features = NULL,
  ...
)
}
\arguments{
\item{X}{Predictor data frame or matrix}

\item{n_estimators}{Number of TabPFN models to use (default: 4)}

\item{device}{Device to use: "auto", "cpu", or "cuda"}

\item{categorical_features}{Vector of column indices or names to treat as categorical (NULL for auto-detection)}

\item{...}{Additional arguments passed to TabPFNClassifier and TabPFNRegressor}
}
\value{
A tab_pfn_unsupervised model object
}
\description{
Creates an unsupervised model for detecting anomalies using TabPFN's
joint probability estimation. The model uses both classifier and regressor
models to estimate the likelihood of samples under the learned distribution.
}
\examples{
\dontrun{
library(rtabpfn)

# Prepare data
X <- mtcars[, c("cyl", "disp", "hp", "wt")]

# Train unsupervised model
model <- tab_pfn_unsupervised(X, n_estimators = 4)

# Detect anomalies
scores <- anomaly_scores(model, X, n_permutations = 10)
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesian_optimization.R
\name{tab_pfn_bayesian_optimization}
\alias{tab_pfn_bayesian_optimization}
\title{Bayesian optimization with TabPFN as the surrogate model}
\usage{
tab_pfn_bayesian_optimization(
  fun,
  bounds,
  init_X = NULL,
  init_y = NULL,
  n_init = 5,
  n_iter = 20,
  maximize = TRUE,
  device = "auto",
  n_estimators = 1,
  random_state = 0,
  n_candidates = 512,
  top_k = 4,
  n_refine_steps = 8,
  refine_lr = 0.05,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{fun}{An R function taking a named numeric vector (one value per bound)
and returning a single numeric objective value.}

\item{bounds}{A named list of `c(lower, upper)` pairs, one per dimension of
the search space.}

\item{init_X}{Optional matrix of initial points in the original space.}

\item{init_y}{Optional numeric vector of objective values for `init_X`.}

\item{n_init}{Number of random initial points (used when `init_X` is `NULL`).}

\item{n_iter}{Number of Bayesian optimization iterations.}

\item{maximize}{Logical. If `TRUE` (default) maximize the objective, else
minimize it.}

\item{device}{Device: "auto", "cpu", or "cuda".}

\item{n_estimators}{Number of TabPFN estimators (default 1).}

\item{random_state}{Random seed.}

\item{n_candidates}{Number of random candidates screened per round.}

\item{top_k}{Number of candidates refined by gradient ascent.}

\item{n_refine_steps}{Gradient steps on the candidate coordinates.}

\item{refine_lr}{Learning rate for the refinement steps.}

\item{verbose}{Logical. Print progress for each iteration.}

\item{...}{Additional arguments passed to `TabPFNRegressor`.}
}
\value{
A list with elements `X` (all evaluated points, original space),
  `y` (their objective values), `best_x` (named vector), and `best_y`.
}
\description{
Optimizes a black-box objective with TabPFN as the surrogate model and
differentiable Expected Improvement (PFNs4BO). `bounds` are mapped to the
unit hypercube internally; `fun` is always called in the original space.
}
\examples{
\dontrun{
# Minimize f(x) = (x - 0.3)^2 over [0, 1] (true minimum at 0.3).
bo <- tab_pfn_bayesian_optimization(
  fun = function(x) (x["x"] - 0.3)^2,
  bounds = list(x = c(0, 1)),
  n_init = 5, n_iter = 10, maximize = FALSE
)
bo$best_x
bo$best_y
}
}

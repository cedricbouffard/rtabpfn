% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesian_optimization.R
\name{tab_pfn_bo_suggest}
\alias{tab_pfn_bo_suggest}
\title{One Bayesian optimization acquisition step}
\usage{
tab_pfn_bo_suggest(
  X,
  y,
  device = "auto",
  n_estimators = 1,
  random_state = 0,
  n_candidates = 512,
  top_k = 4,
  n_refine_steps = 8,
  refine_lr = 0.05,
  ...
)
}
\arguments{
\item{X}{Matrix of observed inputs of shape `(n, d)`, scaled to `[0, 1]^d`.}

\item{y}{Numeric vector of observed objective values of length `n`, in the
surrogate (maximization) space (negate your values to minimize).}

\item{device}{Device: "auto", "cpu", or "cuda".}

\item{n_estimators}{Number of TabPFN estimators (default 1).}

\item{random_state}{Random seed.}

\item{n_candidates}{Number of random candidates screened per round.}

\item{top_k}{Number of candidates refined by gradient ascent.}

\item{n_refine_steps}{Gradient steps on the candidate coordinates.}

\item{refine_lr}{Learning rate for the refinement steps.}

\item{...}{Additional arguments passed to `TabPFNRegressor`.}
}
\value{
A numeric vector (length `d`) in `[0, 1]^d` proposing the next point.
}
\description{
Runs a single round of TabPFN Bayesian optimization: fits a differentiable
TabPFN surrogate on the observed points and proposes the next point by
gradient ascent on Expected Improvement. Use this if you want to drive the
loop yourself; otherwise use [tab_pfn_bayesian_optimization()].
}

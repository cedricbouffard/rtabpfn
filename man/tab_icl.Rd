% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parsnip-tabicl.R
\name{tab_icl}
\alias{tab_icl}
\alias{update.tab_icl}
\title{TabICL Model for tidymodels}
\usage{
tab_icl(
  mode = "unknown",
  engine = "tabicl",
  n_estimators = 8,
  norm_methods = NULL,
  feat_shuffle_method = "latin",
  batch_size = 8,
  device = "auto",
  random_state = 42,
  verbose = FALSE,
  ...
)

\method{update}{tab_icl}(
  object,
  parameters = NULL,
  n_estimators = NULL,
  batch_size = NULL,
  device = NULL,
  random_state = NULL,
  fresh = FALSE,
  ...
)
}
\arguments{
\item{mode}{A single character string for the prediction mode: "classification" or "regression"}

\item{engine}{A single character string specifying the computational engine. For TabICL, this is always "tabicl"}

\item{n_estimators}{Number of ensemble members (integer, default: 8)}

\item{norm_methods}{Normalization methods to try (default: NULL)}

\item{feat_shuffle_method}{Feature permutation strategy (default: "latin")}

\item{batch_size}{Batch size for ensemble processing (integer, default: 8)}

\item{device}{Device to use: "auto", "cpu", or "cuda"}

\item{random_state}{Random seed for reproducibility}

\item{verbose}{Print detailed information during inference (default: FALSE)}

\item{...}{Additional engine-specific arguments}
}
\value{
A model specification object
}
\description{
A TabICL (Tabular In-Context Learning) model that can be used
  with the tidymodels ecosystem (parsnip, workflows, tune, etc.)
}
\examples{
\dontrun{
library(tidymodels)

# Regression model
tab_icl_reg_spec <- tab_icl(mode = "regression") \%>\%
  set_engine("tabicl") \%>\%
  fit(mpg ~ ., data = mtcars)

# Classification model
tab_icl_cls_spec <- tab_icl(mode = "classification") \%>\%
  set_engine("tabicl") \%>\%
  fit(Species ~ ., data = iris)
}
}

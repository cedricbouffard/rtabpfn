% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/time_series.R
\name{tab_pfn_time_series}
\alias{tab_pfn_time_series}
\title{Train a TabPFN Time Series Forecasting Model}
\usage{
tab_pfn_time_series(
  train_df,
  prediction_length,
  quantiles = c(0.1, 0.5, 0.9),
  date_col = NULL,
  value_col = NULL,
  item_id_col = NULL,
  tabpfn_mode = "local",
  tabpfn_output_selection = "median",
  features = NULL,
  verbose = TRUE
)
}
\arguments{
\item{train_df}{A data frame with time series training data. Must contain:
- A date/datetime column (automatically detected or specified with date_col)
- A value column (automatically detected or specified with value_col)
- Optionally, an item_id column for multiple time series}

\item{prediction_length}{Integer. Number of steps to forecast ahead}

\item{quantiles}{Numeric vector of quantiles for probabilistic forecasting (default: c(0.1, 0.5, 0.9))}

\item{date_col}{Character. Name of the date/datetime column (NULL for auto-detection)}

\item{value_col}{Character. Name of the value column (NULL for auto-detection)}

\item{item_id_col}{Character. Name of the item_id column for multiple time series (NULL for single series)}

\item{tabpfn_mode}{Character. TabPFN mode: "local" (default) or "client"}

\item{tabpfn_output_selection}{Character. Output selection: "median" or "mean"}

\item{features}{List of features to extract. NULL for automatic features.
Available features: "running_index", "calendar", "seasonal"}

\item{verbose}{Logical. Print progress information (default: TRUE)}

\item{...}{Additional arguments passed to TabPFNTimeSeriesPredictor}
}
\value{
A tab_pfn_time_series model object
}
\description{
Creates a zero-shot time series forecasting model using TabPFN-TS. The model
requires no training and can generate forecasts for both point predictions
and probabilistic forecasts using quantiles.
}
\examples{
\dontrun{
library(rtabpfn)
library(dplyr)
library(lubridate)

# Create sample time series data
dates <- seq(as.Date("2020-01-01"), as.Date("2022-12-31"), by = "day")
values <- sin(seq(0, 2*pi, length.out = length(dates))) * 10 + rnorm(length(dates), 0, 1)
ts_data <- tibble(date = dates, value = values)

# Train forecasting model
model <- tab_pfn_time_series(ts_data, prediction_length = 30)

# Make forecasts
forecasts <- predict(model, ts_data)
}
}

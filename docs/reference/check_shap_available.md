# Check if SHAP is available

Checks if tabpfn-extensions is installed and available for SHAP
calculations.

## Usage

``` r
check_shap_available()
```

## Value

Logical indicating if SHAP is available

## Examples

``` r
if (FALSE) { # \dontrun{
check_shap_available()
} # }
```
